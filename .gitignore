tags
test.sh
.luarc.json
nvim

spell/

# Machine-local overlay (work laptop extras, etc). Deployed by symlinking an
# external repo to lua/overlay; never committed here. See doc/overlay.md.
/lua/overlay
/lua/overlay.lua

# Plugin versions are intentionally not pinned in this repo, so each host can
# resolve its own. Delete this line if you'd rather lock versions in git.
lazy-lock.json

.DS_Store
